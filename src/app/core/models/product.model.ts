export interface Product { 
    id: string; 
    title: string; 
    image: string; 
    price: number;
    description: string;
}

export interface ProductEntity {
    id: string;
    title: string;
    description: string;
    image: string;
    price: number;
  }